# ⚠️ 이 워크플로우는 master-issue-orchestrator.yml로 통합되었습니다.
# MCP Sub-agents는 마스터 오케스트레이터에서 자동으로 관리됩니다.

name: "🔗 MCP Sub-agents Integration (DEPRECATED)"

on:
  # 기본적으로 비활성화 - 마스터 오케스트레이터가 Sub-agents를 관리
  workflow_dispatch:
    inputs:
      show_migration_info:
        description: '통합 정보 표시'
        required: false
        default: true
        type: boolean

jobs:
  integration-migration-notice:
    runs-on: ubuntu-latest
    steps:
      - name: 📢 MCP Sub-agents 통합 안내
        run: |
          echo "⚠️ MCP Sub-agents 시스템이 마스터 오케스트레이터로 통합되었습니다"
          echo ""
          echo "🤖 통합된 6개 Sub-agents:"
          echo "  1. issue-manager: 이슈 분석 및 분류"
          echo "  2. code-quality-reviewer: 코드 품질 검토"
          echo "  3. database-migration-manager: DB 마이그레이션"
          echo "  4. test-automation-specialist: 자동화 테스트"
          echo "  5. deployment-manager: 배포 관리"
          echo "  6. workflow-orchestrator: 워크플로우 통합"
          echo ""
          echo "🚀 자동 오케스트레이션 기능:"
          echo "  - 이슈 우선순위별 동적 라우팅"
          echo "  - MCP 프로토콜 표준화 통신"
          echo "  - 실시간 상태 모니터링"
          echo "  - 조건부 에이전트 활성화"
          echo "  - 병렬/순차 처리 자동 선택"
          
      - name: 🔄 사용법 안내
        if: inputs.show_migration_info == 'true'
        run: |
          echo "📍 새로운 사용법:"
          echo ""
          echo "🎯 자동 오케스트레이션 (권장):"
          echo "  - 이슈 생성 → 자동 우선순위 분석 → 적절한 Sub-agents 선택"
          echo "  - P0: issue-manager + deployment-manager + test-automation"
          echo "  - P1: issue-manager + code-quality-reviewer + deployment-manager"
          echo "  - P2/P3: issue-manager + code-quality-reviewer"
          echo ""
          echo "🔧 수동 트리거 (필요시):"
          echo "  - master-issue-orchestrator.yml의 workflow_dispatch 사용"
          echo "  - 특정 Sub-agents 조합 선택 가능"
          echo ""
          echo "📊 개선된 기능:"
          echo "  - 지능형 Sub-agents 선택"
          echo "  - 실패 시 자동 에이전트 재할당"
          echo "  - 성능 모니터링 및 최적화"
          echo "  - 병목 지점 자동 감지 및 해결"