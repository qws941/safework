# SafeWork Portainer 배포 설정 (메인 설정 파일)
# 모듈화된 설정 파일들을 통합하여 로드

# =============================================================================
# Portainer API 설정
# =============================================================================
PORTAINER_URL="https://portainer.jclee.me"
PORTAINER_TOKEN="ptr_lejbr5d8IuYiEQCNpg2VdjFLZqRIEfQiJ7t0adnYQi8="

# Endpoint 매핑
ENDPOINT_PRODUCTION="3"
ENDPOINT_DEV="2"

# =============================================================================
# Registry 설정
# =============================================================================
LOCAL_REGISTRY="localhost:5000"
PROD_REGISTRY="registry.jclee.me"

# =============================================================================
# 모듈화된 설정 파일 로드
# =============================================================================
# 이 파일은 메인 설정 파일이며, 다른 모듈화된 설정들을 함께 로드합니다:
# - database.env: 데이터베이스 관련 설정
# - redis.env: Redis 캐시 관련 설정
# - application.env: 애플리케이션 보안 및 Flask 설정
# - infrastructure.env: 컨테이너, Health Check, 로깅 설정

# 설정 파일들이 스크립트에서 자동으로 로드됩니다.